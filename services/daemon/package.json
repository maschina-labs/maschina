{
	"name": "@maschina/daemon",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Runs machines on a node. Dials out to the orchestrator and never touches the database.",
	"scripts": {
		"dev": "tsx watch --env-file-if-exists=../../.env src/main.ts",
		"build": "tsdown",
		"start": "node --enable-source-maps dist/main.mjs",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"check:bundle": "node ../../scripts/checks/bundle-deps.mjs"
	},
	"dependencies": {
		"@sentry/node": "catalog:",
		"pino": "catalog:",
		"uuidv7": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@maschina/config": "workspace:*",
		"@maschina/contracts": "workspace:*",
		"@maschina/core": "workspace:*",
		"@maschina/env": "workspace:*",
		"@maschina/telemetry": "workspace:*",
		"@types/node": "catalog:",
		"@vitest/coverage-v8": "catalog:",
		"tsdown": "catalog:",
		"tsx": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
